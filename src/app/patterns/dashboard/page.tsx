import { DashboardBlock } from "@/registry/blocks/dashboard"

/** Live preview of the dashboard block. The block itself is what the registry ships. */
export default function DashboardPattern() {
  return <DashboardBlock />
}
