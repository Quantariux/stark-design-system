import { SearchBlock } from "@/registry/blocks/search"

/** Live preview of the search block. The block itself is what the registry ships. */
export default function SearchPattern() {
  return <SearchBlock />
}
