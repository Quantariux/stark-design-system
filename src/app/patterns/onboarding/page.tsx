import { OnboardingBlock } from "@/registry/blocks/onboarding"

/** Live preview of the onboarding block. The block itself is what the registry ships. */
export default function OnboardingPattern() {
  return <OnboardingBlock />
}
