import { DetailBlock } from "@/registry/blocks/detail"

/** Live preview of the detail block. The block itself is what the registry ships. */
export default function DetailPattern() {
  return <DetailBlock />
}
