import { SettingsBlock } from "@/registry/blocks/settings"

/** Live preview of the settings block. The block itself is what the registry ships. */
export default function SettingsPattern() {
  return <SettingsBlock />
}
